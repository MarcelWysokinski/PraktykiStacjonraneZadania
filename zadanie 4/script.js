const oblicz =()=>{
    var tablica1 = [10, 10, "Halina", 15,10,"Malina"];
    var wynik = tablica1.filter(filtr);
    console.log(wynik);
    }
    const filtr =(numer)=>{
    if(numer === 10){
        return numer;
    }
    }
    oblicz();