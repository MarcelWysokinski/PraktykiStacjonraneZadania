var tab = [1,4,1,8,1,5,2,4,6,1];
    
if (tab != isNaN) {
    var suma = 0;
for (var i = 0; i < tab.length; i++) {
  suma += tab[i];
}
console.log(suma);
}
else {
    console.log("To co zostalo wpisane nie jest liczba");
}